
# Core UI
gradio>=4.44.0

# PDF handling
pymupdf>=1.24.0

# Image processing
Pillow>=10.4.0
opencv-python>=4.10.0
numpy>=1.26.0

# OCR — dots.mocr uses transformers + torch
# IMPORTANT: dots.mocr requires transformers 4.x — version 5.x breaks the model
transformers>=4.44.0,<5.0.0
torch>=2.3.0
qwen-vl-utils>=0.0.8
accelerate>=0.30.0

# Translation (fully local)
argostranslate>=1.9.6

# ── Optional: Flash Attention 2 (Linux + CUDA only, ~30% faster OCR) ────
# Install on Linux servers via: pip install flash-attn --no-build-isolation
# Requires: CUDA toolkit, gcc/g++, ninja-build
# On Windows: not supported — SDPA is used automatically instead.
# flash-attn>=2.5.0